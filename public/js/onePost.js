const saveCommentBtn = document.querySelector('#save-comment-btn');

// Function to save the comment to the database
const saveComment = async (e) => {
    e.preventDefault();
}

document.addEventListener(saveCommentBtn, saveComment);